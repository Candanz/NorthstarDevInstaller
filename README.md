If you want to run this python file yourself

Make sure you run:

`pip install customtkinter requests beautifulsoup4`

To install all dependencies.

Then you can run it with 
`python installer.py`

In the releases tab, you can also find an .exe file that is made with PyInstaller, from this source code.
